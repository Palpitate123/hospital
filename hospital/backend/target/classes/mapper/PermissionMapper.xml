<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.PermissionMapper">
    
    <resultMap id="PermissionResultMap" type="com.hospital.entity.Permission">
        <id property="id" column="id"/>
        <result property="permissionName" column="permission_name"/>
        <result property="permissionCode" column="permission_code"/>
        <result property="description" column="description"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    
    <!-- 根据角色ID查询权限列表 -->
    <select id="selectByRoleId" resultMap="PermissionResultMap">
        SELECT p.* FROM permission p
        LEFT JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
    </select>
    
    <!-- 根据用户ID查询权限列表 -->
    <select id="selectByUserId" resultMap="PermissionResultMap">
        SELECT DISTINCT p.* FROM permission p
        LEFT JOIN role_permission rp ON p.id = rp.permission_id
        LEFT JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>
</mapper>